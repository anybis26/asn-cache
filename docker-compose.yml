services:
  asn-cache:
    build: .
    container_name: asn-cache
    restart: unless-stopped

    ports:
      - "8080:8080"

    volumes:
      - ./data:/data

    environment:
      - DB=/data/asn-cache.db